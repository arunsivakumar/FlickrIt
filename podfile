# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FlickrIt' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FlickrIt
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftyJSON'
  pod 'Bond'
  pod 'Kingfisher', '~> 3.0'

  target 'FlickrItTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlickrItUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
